# 「一句毛选」微信小程序 · 视觉与交互全面审查报告

> 审查日期：2026-08-16 · 审查范围：6 个页面 + 7 个组件 + 全局样式
> 审查维度：字体字号 / 行距字距 / 颜色对比度（WCAG）/ 布局间距 / 可点击区域 / 加载与空状态 / 导航流程
> 参考基准：rpx 换算按 iPhone 375pt 屏（1rpx = 0.5pt）；无障碍对比度按 WCAG 2.1 AA（正文 ≥4.5:1，大字 ≥3:1，触控目标 ≥44×44pt）

---

## 结论速览

整体设计语言统一（毛选红 + 暖金 + 液玻璃），动效克制、`prefers-reduced-motion` 降级到位、骨架屏/空状态体系完整，属于**完成度较高**的作品。但存在 **3 个 P0 级可读性/无障碍缺陷** 与 **若干 P1/P2 级一致性与交互问题**。

| 级别 | 数量 | 核心主题 |
|------|------|----------|
| 🔴 P0 严重 | 3 | 低对比度辅助文字、9-10pt 过小字号、触控目标偏小 |
| 🟠 P1 重要 | 7 | 设计不一致、按钮拥挤、空状态语义、导航标题对齐、文案误导 |
| 🟡 P2 建议 | 4 | 背景重复、SVG 重复编码、手势冲突、误触丢输入 |

---

## 一、字体类型与字号（可读性）

### 🔴 P0-1 多处文字字号低于可读下限（9-10pt）

微信小程序字号建议正文 ≥28rpx（14pt），辅助文字 ≥24rpx（12pt）。当前多处落在 18-22rpx（9-11pt），在真机上明显偏小。

| 元素 | 类名 | 当前 | 建议 | 位置 |
|------|------|------|------|------|
| 稀有度标签 | `.card-rarity` | 18rpx(9pt) | 22rpx 起 | `pages/index/index.wxss` |
| 卡牌编号 | `.card-number` | 20rpx(10pt) | 22rpx | `pages/index/index.wxss` |
| 卡牌出处 | `.card-source-text` | 20rpx(10pt) | 22rpx | `pages/index/index.wxss` |
| 卡牌落款 | `.card-author` | 20rpx(10pt) | 22rpx | `pages/index/index.wxss` |
| 操作栏标签 | `.action-label`(index) | 20rpx(10pt) | 24rpx | `pages/index/index.wxss` |
| 底部 Tab 文字 | `.tab-text` | 20rpx(10pt) | 22-24rpx | `custom-tab-bar/index.wxss` |
| 品牌标语 | `.brand-slogan` | 22rpx(11pt) | 24rpx | `pages/index/index.wxss` |
| 操作栏标签 | `.action-label`(quote-card) | 22rpx(11pt) | 24rpx | `components/quote-card/quote-card.wxss` |

**预期改善**：稀有度标签、卡号、操作按钮文字在真机上清晰可读，降低"凑近看"的阅读负担，尤其对中老年用户群（毛选题材目标用户）友好。

> ✅ 做得好的地方：正文 `quote-text` 40rpx、`card-quote-text` 36rpx、列表 `rank-quote`/`item-quote` 30rpx 字号与层级合理；标题 48rpx/34rpx 分级清晰。

---

## 二、行间距与字间距

行距与字距整体质量较高，仅两处可微调：

- 🟡 **金句正文行高略松散**：`card-quote-text` `line-height:1.78`（36rpx→64rpx）、`quote-text` `line-height:1.72`（40rpx→69rpx）。对 1-2 行的短金句偏松，垂直留白过多，卡片中段显空。建议短句场景下调至 1.55-1.65。
- 🟡 **字间距不统一**：正文 `letter-spacing:1-2rpx`（衬线金句）与无衬线 UI 文字（0 或负值）区分得当，但 `brand-title` `12rpx` 与 `stamp-char` 无间距之间的对比可再权衡。

**预期改善**：短金句卡面更紧凑饱满，减少视觉留白空洞。

---

## 三、颜色对比度（无障碍 · 有实测数据）

实测对比度（对米白底 `#FBF9F6` / 纯白 `#FFFFFF`）：

| 色值 | 变量 | 对比度 | AA 判定 |
|------|------|--------|---------|
| `#A28F7D` | `--ink-3` | **2.96 / 3.11** | ❌ 正文+大字均不合格 |
| `#9C8F84` | tab 未选中 | **2.99 / 3.15** | ❌ 不合格 |
| `#C9943F` | `--gold` | **2.56 / 2.70** | ❌ 不合格（作文字时） |
| `#A5762C` | `--gold-deep` | 3.82 / 4.02 | ⚠️ 仅大字合格 |
| `#8A7564` | `--ink-2` | 4.16 / 4.37 | ⚠️ 小字略低于 4.5 |
| `#6E5F55` | — | 5.82 / 6.12 | ✅ 合格 |
| `#CF3A32` | `--primary` | 4.64 / 4.88 | ✅ 正文合格 |
| `#2A1E18`/`#241A15` | `--ink` | 15.4 / 16.2 | ✅ 优秀 |

### 🔴 P0-2 `#A28F7D` 对比度严重不足（2.96:1）

该色被广泛用于**信息性小字**，实测远低于 4.5:1，甚至低于大字 3:1 门槛：

- `.brand-slogan`（首页标语）
- `.card-number`（卡号）、`.card-author`（落款）
- `.rank-subtitle`（排行副标题）
- `.item-time`（收藏/点赞时间）
- `.menu-count`（我的页数量）

**建议**：将 `--ink-3` 由 `#A28F7D` 加深至 `#8A7564` 或 `#7A6A5C`（目标 ≥4.5:1），仅保留纯装饰场景用浅色。

### 🔴 P0-3 金色 `#C9943F` 作信息文字不可读（2.56:1）

`rank-number.top`（TOP3 金色名次数字）、收藏态标签文字用 `#C9943F`/`#A5762C`，在浅底上几乎看不清。

**建议**：金色仅用于图标与装饰边框；作为文字时改用 `#A5762C` 并加深至 `#8C6A1F`（≥4.5:1），或加大字重并补足对比度。

### 🟠 P1 `#9C8F84` 未选中 Tab 文字（2.99:1）

底部 Tab 未选中态文字对比度不足。建议加深至 `#8A7564` 附近。

**预期改善**：全部辅助文字达到 WCAG AA，弱视与阳光下使用可读，同时保持当前暖色审美（加深而非换色）。

---

## 四、页面布局与组件间距

### 🟠 P1-1 首页操作栏 5 按钮拥挤 + 数字溢出风险

`action-bar` 宽度 690rpx，塞入 5 个按钮（再拆一个/点赞/收藏/转发/海报），每项仅约 133rpx 宽，标签 20rpx。且点赞数直接拼接文字：

```xml
<text class="action-label">{{liked ? '已赞' : '点赞'}} {{quote.stat.total || 0}}</text>
```

当点赞数达 4-5 位（如 "点赞 12345"）时 `white-space:nowrap` 会溢出按钮边界。

**建议**：① 将"转发"与"海报"合并为一个二级菜单（长按或点击展开）；② 点赞数 ≥10000 用 `1.2万` 缩写（`formatNumber` 已支持）；③ 标签 `max-width` + `text-overflow:ellipsis`。

### 🟠 P1-2 同一金句两处排版风格不一致

| 场景 | 字号 | 对齐 | 引号 |
|------|------|------|------|
| 首页盲盒卡面 | 36rpx | 居中 | 「」上下全角 |
| 详情页 quote-card | 40rpx | 左对齐 | " 左上角装饰 |

同一内容在首页与详情页呈现两种截然不同的版式，破坏品牌一致性。

**建议**：统一为一种"金句展示版式"，差异仅保留在载体装饰（卡面 vs 卡片），正文排版（字号、行高、对齐、引号）保持一致。

### 🟡 P2-1 各页面 aurora 渐变背景重复粘贴 6 次

`index/rank/mine/detail/favorites/likes` 六个页面的 `.container` 都重复定义了同一段 8 行 `radial-gradient` 背景；而 `app.wxss` 已提供 `.aurora-bg` 类却未被复用。

**建议**：页面 `.container` 直接复用 `app.wxss` 的 `.aurora-bg`，或抽出 `page` 级变量，消除 6 份重复，未来调色只改一处。

---

## 五、按钮与交互元素可点击区域

### 🔴 P0-4 触控目标低于 44×44pt 标准

| 元素 | 当前尺寸 | 换算 | 建议 |
|------|----------|------|------|
| 导航返回 `.nav-back` | 64×64rpx | 32×32pt | ≥88×88rpx（44pt），至少扩大到 80rpx |
| 排行收藏 `.rank-favorite` | 64×64rpx | 32×32pt | ≥88rpx |
| 登录弹窗"暂不完善" `.login-cancel` | 高约 60rpx | 30pt | padding 增至 ≥40rpx |
| 空状态按钮 `.empty-btn` | 高约 62rpx | 31pt | padding 增至 ≥32rpx 0 |

**建议**：视觉尺寸可保持小巧，但通过 `::after` 伪元素或负 padding 扩大**实际热区**到 44pt（iOS/Android 无障碍建议），避免误触与"点不到"。

**预期改善**：返回、收藏等高频操作不再"点不准"，符合平台触控规范。

---

## 六、加载与空状态反馈

✅ 骨架屏体系完整（首页/排行/收藏/点赞均有 shimmer 骨架 + `prefers-reduced-motion` 降级），空状态文案与插图到位。

### 🟠 P1-3 详情页缺加载骨架 + 不存在时白屏 1.5s

`detail.wxml` 中 `quote-card` 由 `wx:if="{{quote}}"` 控制，但无骨架占位。`loadQuote` 中当金句不存在时，仅 `showToast` 后 `setTimeout` 1.5s 才跳转——这 1.5 秒页面是**空白**。

**建议**：① 加详情页骨架；② 不存在时立即显示空状态（复用 `empty-state` 的 `error` 预设），而非 toast + 白屏等待。

### 🟠 P1-4 排行加载失败显示"暂无排行"而非错误态

`rank.js` 的 `loadRank` catch 分支仅 `showToast('排行加载失败')`，列表保持空 → 空状态组件渲染 `type="rank"` 的"暂无排行"文案，误导用户以为真的没数据。

**建议**：失败时切换为 `empty-state type="error"`（组件已内置 network/error 预设，但从未使用），按钮"重试"直接重载。

### 🟠 P1-5 "轻启锦盒 · 拆一句" 文案与自动拆卡矛盾

首页 `onLoad` 会**自动**执行 `startDrawSequence()`，因此 `drawState === 'idle'`（显示"轻启锦盒 · 拆一句"提示）的阶段几乎一闪而过。用户实际上不需要"轻启"，提示文案存在误导。

**建议**：若保留自动拆卡，将提示改为"正在为您拆一句…"或直接移除 idle 提示；若希望用户主动拆卡，则移除 `onLoad` 的自动调用。

---

## 七、导航流程

### 🟠 P1-6 导航栏标题在有返回按钮时未居中

`nav-bar` 的 `.nav-content` 为 `padding:0 176rpx 0 16rpx`，标题 `flex:1` 左对齐。有返回按钮的页面（详情/收藏/点赞）标题视觉偏左，未居中。

**建议**：返回按钮用 `position:absolute` 左置，标题 `text-align:center` 真居中（`176rpx` 为避让右上胶囊，可保持两侧对称留白）。

### 🟡 P2-2 排行页 swiper 与 scroll-view 手势潜在冲突

`swiper` 横向滑动切换"今日/本周/本月"，内层 `scroll-view` 纵向滚动。两者手势可共存，但快速斜向滑动时可能误触发。当前已有 `refresher` 下拉刷新，若需可加 `disable-touch` 微调，属低优先级。

---

## 八、其他（P2）

- 🟡 **P2-3 登录弹窗误触关闭丢失输入**：`login-modal` 的遮罩 `bindtap="onOverlayTap"` 直接关闭，若用户已填昵称误触遮罩会丢失。建议遮罩点击仅当无输入时关闭，或有输入时二次确认。
- 🟡 **P2-4 图标 SVG 每次状态变化重复编码**：`app-icon` 的 `createIconSource` 在 `name/color/filled` 任一变化时重新 `encodeBase64`。点赞/收藏态切换会频繁触发，可对 `(name,color,filled)` 三元组做 Map 缓存。

---

## 附：修复优先级建议

| 批次 | 项目 | 影响面 | 改动量 |
|------|------|--------|--------|
| 第一批 | P0-1 字号 / P0-2 P0-3 对比度 / P0-4 触控热区 | 全量用户可读性+无障碍 | 小（纯 CSS token 与数值） |
| 第二批 | P1-1 按钮拥挤 / P1-2 排版统一 | 首页核心体验 + 品牌一致性 | 中（涉及组件结构） |
| 第三批 | P1-3 详情骨架 / P1-4 错误态 / P1-5 文案 | 边界体验 | 小（复用已有 empty-state） |
| 第四批 | P1-6 标题居中 / P2 系列 | 细节打磨 | 小 |

> 核心结论：本项目**视觉完成度与动效打磨已属上乘**，主要短板集中在"小字号 + 低对比度"这一无障碍硬伤，以及"双卡片版式不一致"的品牌统一问题。建议优先用设计令牌（CSS 变量）统一修复第一批，收益最大、风险最低。
