/* ============ 容器 & 背景（浅色液玻璃 · 多彩背景透出磨砂玻璃） ============ */
.container {
  min-height: 100vh;
  background: var(--aurora-bg);
  color: #2A1E18;
  padding-bottom: calc(150rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  overflow: hidden;
}

/* ============ 背景粒子（暖金光尘，轻量化） ============ */
.bg-particles {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
  z-index: 0;
}

.particle {
  position: absolute;
  width: 6rpx;
  height: 6rpx;
  background: #E0B25E;
  border-radius: 50%;
  opacity: 0;
  box-shadow: 0 0 10rpx rgba(201, 148, 63, 0.55);
}

.particle-0 { left: 15%; top: 30%; animation: particleFloat 1.2s ease-out forwards; }
.particle-1 { left: 30%; top: 25%; animation: particleFloat 1.5s ease-out forwards; }
.particle-2 { left: 50%; top: 20%; animation: particleFloat 1.3s ease-out forwards; }
.particle-3 { left: 70%; top: 25%; animation: particleFloat 1.6s ease-out forwards; }
.particle-4 { left: 85%; top: 30%; animation: particleFloat 1.2s ease-out forwards; }
.particle-5 { left: 20%; top: 45%; animation: particleFloat 1.4s ease-out forwards; }
.particle-6 { left: 40%; top: 40%; animation: particleFloat 1.7s ease-out forwards; }
.particle-7 { left: 60%; top: 40%; animation: particleFloat 1.3s ease-out forwards; }
.particle-8 { left: 80%; top: 45%; animation: particleFloat 1.5s ease-out forwards; }
.particle-9 { left: 25%; top: 60%; animation: particleFloat 1.2s ease-out forwards; }
.particle-10 { left: 55%; top: 55%; animation: particleFloat 1.6s ease-out forwards; }
.particle-11 { left: 75%; top: 60%; animation: particleFloat 1.4s ease-out forwards; }

@keyframes particleFloat {
  0% { opacity: 0; transform: translateY(0) scale(0); }
  20% { opacity: 0.9; transform: translateY(-20rpx) scale(1); }
  100% { opacity: 0; transform: translateY(-120rpx) scale(0.3); }
}

/* ============ 品牌区 ============ */
.brand-area {
  padding-top: 64rpx;
  padding-bottom: 10rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  z-index: 2;
}

/* Logo（方案 C · 毛选印记）：玻璃 squircle + 红印章竖排「毛选」+ 金色落款点 */
.brand-stamp {
  position: relative;
  width: 96rpx;
  height: 96rpx;
  margin-bottom: 18rpx;
  border-radius: 22rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.82) 0%, rgba(255, 255, 255, 0.54) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.8);
  box-shadow:
    0 10rpx 26rpx rgba(120, 90, 60, 0.16),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
}

.stamp-seal {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 68rpx;
  height: 68rpx;
  transform: translate(-50%, -50%);
  border-radius: 15rpx;
  background: linear-gradient(135deg, #D8483E 0%, #CF3A32 55%, #AD2822 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.35);
  box-shadow:
    0 6rpx 14rpx rgba(138, 43, 36, 0.3),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.4);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  line-height: 1.05;
}

.stamp-char {
  font-size: 27rpx;
  font-weight: 800;
  color: #FFF9F4;
  font-family: 'Noto Serif CJK SC', 'Songti SC', serif;
}

.stamp-dot {
  position: absolute;
  right: 8rpx;
  bottom: 8rpx;
  width: 7rpx;
  height: 7rpx;
  border-radius: 2rpx;
  background: linear-gradient(135deg, #D8AC5C 0%, #A5762C 100%);
  box-shadow: 0 0 6rpx rgba(201, 148, 63, 0.6);
}

.brand-title {
  font-size: 48rpx;
  font-weight: 800;
  color: #241A15;
  letter-spacing: 12rpx;
  margin-bottom: 14rpx;
  font-family: 'Noto Serif CJK SC', 'Songti SC', serif;
}

.brand-divider {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.divider-line {
  width: 50rpx;
  height: 2rpx;
  background: linear-gradient(90deg, transparent, #C9943F, transparent);
}

.divider-dot {
  width: 6rpx;
  height: 6rpx;
  background: #C9943F;
  border-radius: 50%;
  box-shadow: 0 0 8rpx rgba(201, 148, 63, 0.5);
}

.brand-slogan {
  font-size: 24rpx;
  color: #7A6A5C;
  letter-spacing: 2rpx;
  font-weight: 500;
}

.daily-summary {
  min-height: 88rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.daily-label,
.streak-label {
  min-height: 48rpx;
  padding: 0 18rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  font-size: 24rpx;
  font-weight: 600;
  color: #6E5F55;
  background: rgba(255, 255, 255, 0.62);
  border: 1rpx solid rgba(255, 255, 255, 0.78);
}

.daily-label {
  color: #AD2822;
}

/* ============ 礼盒区 ============ */
.box-area {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  z-index: 2;
  padding-top: 48rpx;
  min-height: 620rpx;
}

.box-area.state-opening,
.box-area.state-revealed {
  min-height: 800rpx;
  padding-top: 0;
}

/* ---- 中国风锦盒（天地盖 · 描金 · 方印） ---- */
.box-wrap {
  width: 340rpx;
  height: 340rpx;
  position: relative;
  transition: transform 200ms ease, opacity 360ms ease;
}

.box-area.state-opening .box-wrap,
.box-area.state-revealed .box-wrap {
  transform: translateY(-48rpx) scale(0.7);
  opacity: 0.35;
  pointer-events: none;
  transition: transform 520ms cubic-bezier(0.22, 1, 0.36, 1), opacity 360ms ease;
}

.box-pressed {
  transform: scale(0.97);
}

.box-wrap.is-shaking {
  animation: boxShake 500ms ease-in-out;
}

@keyframes boxShake {
  0%, 100% { transform: rotate(0) translateX(0); }
  15% { transform: rotate(-3deg) translateX(-6rpx); }
  30% { transform: rotate(3deg) translateX(6rpx); }
  45% { transform: rotate(-2deg) translateX(-4rpx); }
  60% { transform: rotate(2deg) translateX(4rpx); }
  75% { transform: rotate(-1deg) translateX(-2rpx); }
  90% { transform: rotate(1deg) translateX(1rpx); }
}

/* 天盖 */
.box-lid {
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 304rpx;
  height: 108rpx;
  z-index: 6;
  transition: transform 800ms cubic-bezier(0.34, 1.56, 0.64, 1), opacity 600ms ease 400ms;
}

.lid-face {
  position: absolute;
  inset: 0;
  border-radius: 10rpx 10rpx 4rpx 4rpx;
  background: linear-gradient(165deg, #E2554C 0%, #CF3A32 42%, #AD2822 100%);
  border: 2rpx solid rgba(216, 172, 92, 0.85);
  box-shadow:
    0 12rpx 28rpx rgba(120, 40, 30, 0.28),
    inset 0 2rpx 0 rgba(255, 220, 180, 0.35),
    inset 0 -2rpx 0 rgba(100, 20, 16, 0.25);
  overflow: hidden;
}

.gold-frame {
  position: absolute;
  inset: 10rpx;
  border: 1rpx solid rgba(232, 196, 120, 0.55);
  border-radius: 4rpx;
  pointer-events: none;
}

/* 四角回纹简笔 */
.corner {
  position: absolute;
  width: 22rpx;
  height: 22rpx;
  border-color: rgba(232, 196, 120, 0.75);
  border-style: solid;
  pointer-events: none;
}

.corner-tl {
  top: 16rpx;
  left: 16rpx;
  border-width: 2rpx 0 0 2rpx;
}

.corner-tr {
  top: 16rpx;
  right: 16rpx;
  border-width: 2rpx 2rpx 0 0;
}

.corner-bl {
  bottom: 16rpx;
  left: 16rpx;
  border-width: 0 0 2rpx 2rpx;
}

.corner-br {
  bottom: 16rpx;
  right: 16rpx;
  border-width: 0 2rpx 2rpx 0;
}

/* 盖面方印 */
.lid-seal {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6rpx;
  background: linear-gradient(145deg, #FFF8F0 0%, #F3E2C8 100%);
  border: 2rpx solid rgba(173, 40, 34, 0.9);
  box-shadow:
    0 4rpx 10rpx rgba(80, 20, 16, 0.25),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.7);
}

.lid-seal-text {
  font-size: 32rpx;
  font-weight: 800;
  color: #AD2822;
  font-family: 'Noto Serif CJK SC', 'Songti SC', serif;
  line-height: 1;
}

.box-lid.lid-fly {
  animation: lidPop 800ms cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

@keyframes lidPop {
  0% { transform: translateX(-50%) translateY(0) rotate(0deg); opacity: 1; }
  35% { transform: translateX(-50%) translateY(-72rpx) rotate(-6deg); opacity: 1; }
  100% { transform: translateX(-50%) translateY(-200rpx) rotate(-18deg); opacity: 0; }
}

/* 地盒 */
.box-body {
  position: absolute;
  top: 96rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 300rpx;
  height: 228rpx;
  z-index: 3;
}

.box-face {
  position: absolute;
  inset: 0;
  border-radius: 4rpx 4rpx 16rpx 16rpx;
  background:
    radial-gradient(ellipse at 50% 20%, rgba(207, 58, 50, 0.08), transparent 55%),
    linear-gradient(180deg, #FBF4EC 0%, #F0E2D4 55%, #E8D5C2 100%);
  border: 2rpx solid rgba(201, 148, 63, 0.55);
  box-shadow:
    0 18rpx 40rpx rgba(120, 70, 40, 0.18),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.85);
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.body-frame {
  inset: 14rpx;
  border-color: rgba(201, 148, 63, 0.35);
}

.body-motif {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.motif-line {
  width: 36rpx;
  height: 2rpx;
  background: linear-gradient(90deg, transparent, rgba(173, 40, 34, 0.45), transparent);
}

.motif-char {
  font-size: 28rpx;
  font-weight: 700;
  letter-spacing: 8rpx;
  color: rgba(173, 40, 34, 0.55);
  font-family: 'Noto Serif CJK SC', 'Songti SC', serif;
}

/* 开盒柔光 */
.box-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 180rpx;
  height: 180rpx;
  background: radial-gradient(circle, rgba(255, 220, 170, 0.55) 0%, rgba(201, 148, 63, 0.2) 45%, transparent 72%);
  border-radius: 50%;
  animation: glowPulse 600ms ease-out;
  pointer-events: none;
}

@keyframes glowPulse {
  0% { transform: translate(-50%, -50%) scale(0.4); opacity: 0; }
  40% { opacity: 1; }
  100% { transform: translate(-50%, -50%) scale(1.8); opacity: 0; }
}

/* 待拆提示 */
.box-hint {
  position: absolute;
  bottom: -56rpx;
  left: 50%;
  transform: translateX(-50%);
  white-space: nowrap;
  z-index: 5;
}

.box-hint-text {
  font-size: 24rpx;
  color: #6E5F55;
  letter-spacing: 2rpx;
  font-weight: 500;
}

/* ============ 开盒柔光柱 ============ */
.light-beam {
  position: absolute;
  top: 200rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 160rpx;
  height: 420rpx;
  background: linear-gradient(180deg,
    rgba(255, 220, 170, 0.28) 0%,
    rgba(201, 148, 63, 0.1) 40%,
    transparent 100%);
  clip-path: polygon(42% 0%, 58% 0%, 88% 100%, 12% 100%);
  z-index: 3;
  animation: beamGrow 600ms ease-out;
  pointer-events: none;
}

.light-beam.beam-fade {
  animation: beamFade 800ms ease-out forwards;
}

@keyframes beamGrow {
  0% { opacity: 0; transform: translateX(-50%) scaleY(0); transform-origin: top; }
  100% { opacity: 1; transform: translateX(-50%) scaleY(1); transform-origin: top; }
}

@keyframes beamFade {
  0% { opacity: 1; }
  100% { opacity: 0; }
}

/* ============ 卡牌舞台 ============ */
.card-stage {
  width: 100%;
  display: flex;
  justify-content: center;
  position: relative;
  z-index: 4;
  margin-top: 20rpx;
  perspective: 1500rpx;
}

.box-area.state-opening .card-stage,
.box-area.state-revealed .card-stage {
  position: absolute;
  top: 28rpx;
  left: 0;
  margin-top: 0;
}

.card-stage.card-rising {
  animation: cardRise 700ms cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

.card-stage.card-up {
  animation: cardFloat 3s ease-in-out 700ms infinite;
}

@keyframes cardRise {
  0% { opacity: 0; transform: translateY(120rpx) scale(0.6); }
  60% { opacity: 1; transform: translateY(-10rpx) scale(1.05); }
  100% { opacity: 1; transform: translateY(0) scale(1); }
}

@keyframes cardFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6rpx); }
}

/* 3D 翻转容器 */
.card-3d {
  width: 560rpx;
  height: 720rpx;
  position: relative;
  transform-style: preserve-3d;
  transition: transform 800ms cubic-bezier(0.4, 0, 0.2, 1);
}

.card-3d.card-flipped {
  transform: rotateY(180deg);
}

/* 卡背（玻璃 + 红色透光） */
.card-back {
  position: absolute;
  inset: 0;
  backface-visibility: hidden;
  background:
    radial-gradient(ellipse at 50% 40%, rgba(207, 58, 50, 0.3) 0%, transparent 62%),
    linear-gradient(135deg, rgba(255, 250, 246, 0.9) 0%, rgba(250, 234, 224, 0.78) 50%, rgba(245, 226, 212, 0.85) 100%);
  border-radius: 28rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.85);
  box-shadow:
    0 24rpx 70rpx rgba(120, 90, 60, 0.3),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.card-back-frame {
  position: absolute;
  inset: 20rpx;
  border: 2rpx solid rgba(201, 148, 63, 0.5);
  border-radius: 20rpx;
}

.card-back-frame-inner {
  position: absolute;
  inset: 32rpx;
  border: 1rpx solid rgba(201, 148, 63, 0.3);
  border-radius: 16rpx;
}

.card-back-emblem {
  width: 120rpx;
  height: 120rpx;
  background: radial-gradient(circle, rgba(207, 58, 50, 0.22) 0%, transparent 70%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.card-back-emblem-text {
  font-size: 72rpx;
  color: rgba(190, 70, 60, 0.55);
  font-weight: 800;
  font-family: 'Noto Serif CJK SC', 'Songti SC', serif;
  text-shadow: 0 0 20rpx rgba(207, 58, 50, 0.18);
}

.card-back-stars {
  position: absolute;
  inset: 0;
}

.card-star {
  position: absolute;
  width: 12rpx;
  height: 12rpx;
  background: rgba(201, 148, 63, 0.5);
  border-radius: 50%;
  box-shadow: 0 0 8rpx rgba(201, 148, 63, 0.45);
}

.star-1 { top: 60rpx; left: 50rpx; animation: starTwinkle 2s ease-in-out infinite; }
.star-2 { top: 100rpx; right: 60rpx; animation: starTwinkle 2.5s ease-in-out 0.5s infinite; }
.star-3 { bottom: 80rpx; left: 80rpx; animation: starTwinkle 2s ease-in-out 1s infinite; }

@keyframes starTwinkle {
  0%, 100% { opacity: 0.25; transform: scale(0.8); }
  50% { opacity: 1; transform: scale(1.2); }
}

.card-back-shine {
  position: absolute;
  top: 0;
  left: -100%;
  width: 60%;
  height: 100%;
  background: linear-gradient(100deg, transparent 30%, rgba(255, 255, 255, 0.5) 50%, transparent 70%);
  animation: backShine 3s ease-in-out infinite;
}

@keyframes backShine {
  0%, 100% { left: -100%; }
  50% { left: 200%; }
}

/* ---- 卡面（磨砂玻璃卡片） ---- */
.card-front {
  position: absolute;
  inset: 0;
  backface-visibility: hidden;
  transform: rotateY(180deg);
  background:
    radial-gradient(ellipse at 50% 0%, rgba(207, 58, 50, 0.06) 0%, transparent 50%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.94) 0%, rgba(255, 248, 240, 0.84) 46%, rgba(250, 240, 230, 0.9) 100%);
  border-radius: 28rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.9);
  box-shadow:
    0 28rpx 80rpx rgba(120, 90, 60, 0.32),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.95);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* 卡面边框 */
.card-border {
  position: absolute;
  inset: 14rpx;
  border: 2rpx solid rgba(201, 148, 63, 0.4);
  border-radius: 20rpx;
  pointer-events: none;
}

/* 卡面光泽 */
.card-shine {
  position: absolute;
  top: 0;
  left: -60%;
  width: 40%;
  height: 100%;
  background: linear-gradient(100deg, transparent 20%, rgba(255, 255, 255, 0.5) 50%, transparent 80%);
  transform: skewX(-20deg);
  animation: cardShine 4s ease-in-out 1s infinite;
  pointer-events: none;
}

@keyframes cardShine {
  0%, 100% { left: -60%; }
  50% { left: 160%; }
}

/* 卡面顶部 */
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 32rpx 0;
}

/* 稀有度标签 */
.card-rarity {
  font-size: 22rpx;
  font-weight: 700;
  padding: 6rpx 16rpx;
  border-radius: 10rpx;
  letter-spacing: 3rpx;
}

.rarity-legendary {
  background: linear-gradient(135deg, #D8AC4E, #F1CE6E, #C9943F);
  color: #3A2A12;
  box-shadow: 0 4rpx 14rpx rgba(201, 148, 63, 0.38);
}

.rarity-epic {
  background: linear-gradient(135deg, #AD2822, #D8483E, #AD2822);
  color: #FFF9F4;
  box-shadow: 0 4rpx 12rpx rgba(207, 58, 50, 0.34);
}

.rarity-rare {
  background: linear-gradient(135deg, #3D7394, #5B91B4, #3D7394);
  color: #F3FAFF;
}

.rarity-common {
  background: rgba(140, 118, 96, 0.14);
  color: #6E5F55;
  border: 1rpx solid rgba(140, 118, 96, 0.22);
}

.card-number {
  font-size: 22rpx;
  color: #7A6A5C;
  font-weight: 600;
  letter-spacing: 2rpx;
  font-family: Georgia, serif;
}

/* 卡面正文 */
.card-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 20rpx 40rpx;
  position: relative;
}

.card-quote-mark-top {
  font-size: 56rpx;
  color: rgba(207, 58, 50, 0.3);
  font-family: Georgia, 'Songti SC', serif;
  line-height: 1;
  margin-bottom: 12rpx;
  align-self: flex-start;
}

.card-quote-mark-bottom {
  font-size: 56rpx;
  color: rgba(207, 58, 50, 0.3);
  font-family: Georgia, 'Songti SC', serif;
  line-height: 1;
  margin-top: 12rpx;
  align-self: flex-end;
}

.card-quote-text {
  font-size: 36rpx;
  line-height: 1.65;
  font-weight: 600;
  color: #241A15;
  text-align: center;
  font-family: 'Noto Serif CJK SC', 'Songti SC', 'PingFang SC', serif;
  letter-spacing: 2rpx;
}

/* 卡面底部 */
.card-footer {
  padding: 0 32rpx 28rpx;
}

.card-source-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14rpx;
  margin-bottom: 20rpx;
}

.card-source-line {
  flex: 1;
  height: 1rpx;
  background: linear-gradient(90deg, transparent, rgba(201, 148, 63, 0.6), transparent);
  max-width: 80rpx;
}

.card-source-text {
  font-size: 22rpx;
  color: #6E5F55;
  font-weight: 600;
  letter-spacing: 2rpx;
  white-space: nowrap;
}

.card-seal-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.card-seal {
  width: 52rpx;
  height: 52rpx;
  background: linear-gradient(135deg, #D8483E, #AD2822);
  border-radius: 10rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  transform: rotate(-6deg);
  border: 1rpx solid rgba(255, 255, 255, 0.4);
  box-shadow: 0 4rpx 12rpx rgba(207, 58, 50, 0.32);
}

.card-seal-text {
  font-size: 22rpx;
  color: #FFF9F4;
  font-weight: 800;
  font-family: 'Noto Serif CJK SC', 'Songti SC', serif;
  letter-spacing: 1rpx;
}

.card-author {
  font-size: 22rpx;
  color: #7A6A5C;
  font-weight: 500;
  letter-spacing: 2rpx;
  font-style: italic;
}

/* ============ 加载骨架 ============ */
.skeleton-card {
  width: 560rpx;
  height: 720rpx;
  margin-top: 20rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.5) 0%, rgba(255, 255, 255, 0.34) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.6);
  border-radius: 28rpx;
  padding: 60rpx 48rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.skeleton-line {
  height: 36rpx;
  background: rgba(160, 130, 105, 0.16);
  border-radius: 8rpx;
  margin-bottom: 20rpx;
}

.skeleton-line.long { width: 100%; }
.skeleton-line.medium { width: 80%; }
.skeleton-line.short { width: 50%; }

.skeleton-source {
  width: 160rpx;
  height: 24rpx;
  background: rgba(160, 130, 105, 0.12);
  border-radius: 8rpx;
  margin-top: 24rpx;
  align-self: flex-end;
}

/* ============ 操作栏（悬浮液玻璃胶囊） ============ */
.action-bar {
  width: 690rpx;
  margin-top: 28rpx;
  padding: 12rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.8) 0%, rgba(255, 255, 255, 0.58) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.75);
  border-radius: 40rpx;
  box-shadow:
    0 20rpx 55rpx rgba(120, 90, 60, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(30rpx) saturate(180%);
  -webkit-backdrop-filter: blur(30rpx) saturate(180%);
  transition: opacity 180ms ease;
  position: relative;
  z-index: 2;
}

.action-bar.is-disabled {
  opacity: 0.45;
  pointer-events: none;
}

.action-btn {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 88rpx;
  padding: 8rpx 4rpx;
  border-radius: 28rpx;
  transition: transform 160ms ease, opacity 160ms ease, background-color 160ms ease;
}

button.share-btn {
  margin: 0;
  background: transparent;
  border: none;
  line-height: inherit;
  font-size: inherit;
  color: inherit;
}

button.share-btn::after {
  border: none;
}

.action-hover {
  transform: scale(0.9);
  opacity: 0.72;
  background: rgba(207, 58, 50, 0.06);
}

.action-icon-wrap {
  width: 44rpx;
  height: 44rpx;
  margin-bottom: 6rpx;
  transform-origin: center;
}

.action-label {
  font-size: 24rpx;
  color: #6E5F55;
  line-height: 32rpx;
  white-space: nowrap;
  letter-spacing: 1rpx;
  font-weight: 500;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
}

.action-btn.like.active .action-label {
  color: #CF3A32;
  font-weight: 700;
}

.action-btn.favorite.active .action-label {
  color: #8C6A1F;
  font-weight: 700;
}

/* ============ 动画 ============ */
@keyframes scalePop {
  0% { transform: scale(1); }
  45% { transform: scale(1.24); }
  100% { transform: scale(1); }
}

.action-icon-wrap.animate-pop {
  animation: scalePop 240ms cubic-bezier(0.34, 1.56, 0.64, 1);
}

@media (prefers-reduced-motion: reduce) {
  .box-wrap,
  .card-3d,
  .card-stage,
  .action-btn,
  .box-hint-text,
  .card-star,
  .card-back-shine,
  .card-shine,
  .particle,
  .light-beam,
  .box-glow,
  .action-icon-wrap.animate-pop {
    animation: none !important;
    transition: none !important;
  }
}